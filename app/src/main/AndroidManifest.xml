<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.sato.kchan.originalapp">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OriginalApp"
        android:name=".Application">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddExpensesActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".AddFaveActivity"
            android:label="OriginalApp">
        </activity>
        <activity
            android:name=".FaveDetailActivity"
            android:label="OriginalApp" >
        </activity>
        <activity
            android:name=".ExpensesDetailActivity"
            android:label="OriginalApp" >

        </activity>
    </application>
</manifest>